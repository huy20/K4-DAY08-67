# Individual contribution report

## Thông tin

- Họ và tên: Chu Thuỳ Dương
- Mã học viên: 2A202602660
- Nhóm: 67
- Repository/branch: origin/duong, origin/main

## Phần việc đã thực hiện

| Module/deliverable | Việc tôi trực tiếp làm | File/commit/PR | Trạng thái |
|---|---|---|---|
| Xây dựng Golden Dataset | Xây dựng 18 trường hợp kiểm thử chuẩn hóa (question, expected_answer, expected_context) từ dữ liệu thực tế | group_project/evaluation/golden_dataset.json (Commit f02d145, 433ee57) | Done |
| Pipeline đánh giá A/B | Viết kịch bản đánh giá run_evaluation.py chấm điểm tự động theo 4 tiêu chí RAG và xử lý rate limit | group_project/evaluation/run_evaluation.py | Done |
| Thực thi evaluation & xuất kết quả | Chạy đánh giá toàn bộ 18 test cases cho 2 cấu hình (Dense-only vs Hybrid+RRF), trích xuất file kết quả thô | group_project/evaluation/evaluation_results.json | Done |
| Báo cáo đánh giá RAG | Phân tích định lượng số liệu, mổ xẻ nguyên nhân worst performers và đề xuất cải tiến trong RESULT.md | group_project/evaluation/RESULT.md, reports/RESULT.md | Done |

Chỉ kê khai công việc có thể đối chiếu bằng file, commit, pull request, test hoặc kết quả evaluation.

## Quyết định kỹ thuật quan trọng

Mô tả tối đa hai quyết định mà bạn trực tiếp tham gia:

1. **Quyết định:** Thiết kế Golden Dataset bám sát nguyên tắc grounded trên dữ liệu gốc, bao phủ cân bằng giữa cả 2 nhóm tài liệu (văn bản pháp lý và tin tức tính năng game).  
   **Lý do/evidence:** Đảm bảo mỗi câu hỏi đều có expected_context trích xuất trực tiếp từ các đoạn văn bản trong data/standardized/, giúp việc đo lường Context Recall và Context Precision đạt độ tin cậy khoa học cao, phản ánh đúng năng lực retrieval thay vì đánh giá cảm tính.  
   **Trade-off:** Đòi hỏi nhiều công sức đọc hiểu, trích lọc thông tin thủ công và kiểm tra chéo từng đoạn ngữ cảnh để đảm bảo không bị sai lệch dữ liệu.

2. **Quyết định:** Xây dựng barem chấm điểm LLM-as-a-Judge định lượng hóa (thang điểm 0-10 cho Faithfulness, Relevance, Recall và đánh giá nhị phân từng đoạn ngữ cảnh cho Precision) kết hợp cơ chế Exponential Backoff.  
   **Lý do/evidence:** Giúp quá trình đánh giá tự động hóa hoàn toàn nhưng vẫn giữ được tính khắt khe, đồng thời giải quyết triệt để vấn đề nghẽn mạng hoặc vượt hạn mức API (lỗi 429, 503) khi chạy liên tục 36 lượt suy luận.  
   **Trade-off:** Thời gian chạy toàn bộ bộ kiểm thử kéo dài khoảng 4-5 phút, tuy nhiên thu được kết quả đo lường khách quan và toàn diện.

## Kiểm thử và kết quả

- Test hoặc query tôi đã dùng: Chạy `pytest tests/test_acceptance.py` (kiểm tra test_golden_dataset_has_15_grounded_cases và test_evaluation_report_is_completed); chạy thực thi lệnh `python group_project/evaluation/run_evaluation.py --top-k 5`.
- Kết quả trước/sau nếu có: Vượt qua toàn bộ bài kiểm tra acceptance test với 18/18 cases hợp lệ; kết quả evaluation chứng minh Config B (Hybrid + RRF) vượt trội hơn Config A (Average 0.7792 so với 0.7694, Faithfulness đạt 0.9444); báo cáo RESULT.md được hoàn thiện 100% không còn thẻ TODO.
- Lỗi đã phát hiện và cách xử lý: Quá trình ban đầu bị gián đoạn do lỗi quá tải mô hình (503 UNAVAILABLE) từ API; tôi đã tối ưu hóa kịch bản thử nghiệm bằng cách bổ sung cơ chế retry thông minh với độ trễ giữa các lượt chạy và định tuyến sang mô hình hoạt động ổn định gemini-flash-lite-latest.

## Điều còn hạn chế

- Một hạn chế cụ thể của phần tôi làm: Việc đánh giá Context Precision hiện tại phụ thuộc vào khả năng phân định mức độ liên quan của LLM Judge đối với từng đoạn văn bản, đôi khi có thể bị ảnh hưởng bởi độ dài câu trả lời.
- Nếu có thêm thời gian, thay đổi đầu tiên tôi sẽ thực hiện: Mở rộng bộ Golden Dataset lên 50-100 câu hỏi đa dạng hơn và tích hợp thêm các thư viện đánh giá chuyên dụng như Ragas để đối chiếu chéo nhiều nguồn đánh giá.

## Xác nhận đóng góp

Tôi xác nhận nội dung trên phản ánh đúng phần việc của mình và có thể giải thích hoặc chạy lại trong buổi demo.

- Ngày: 25/09/2026
- Tên thành viên: Chu Thuỳ Dương
