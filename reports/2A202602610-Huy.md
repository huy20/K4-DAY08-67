# Individual contribution report

## Thông tin

- Họ và tên: Phùng Quang Minh Huy
- Mã học viên: 2A202602610
- Nhóm: 67
- Repository/branch: origin/huy, origin/main

## Phần việc đã thực hiện

| Module/deliverable | Việc tôi trực tiếp làm | File/commit/PR | Trạng thái |
|---|---|---|---|
| Task 5: Semantic search | Hiện thực hàm tìm kiếm ngữ nghĩa trên ChromaDB, chuẩn hóa cosine distance thành similarity và sắp xếp giảm dần | src/task5_semantic_search.py (Commit 6730609, c17c060) | Done |
| Task 6: Lexical search (BM25) | Cài đặt thuật toán BM25 với RobustBM25Okapi chống âm điểm IDF trên corpus, hỗ trợ tra cứu từ khóa chính xác | src/task6_lexical_search.py (Commit 6730609, c17c060) | Done |
| Task 7: Reranking với RRF | Xây dựng thuật toán Reciprocal Rank Fusion (RRF với k=60) để dung hợp thứ hạng từ nhiều phương pháp tìm kiếm | src/task7_reranking.py (Commit 6730609, c17c060) | Done |
| Task 8: Vectorless search | Tích hợp giải pháp PageIndex tìm kiếm phi vector dựa trên chỉ mục phân cấp tài liệu phục vụ fallback | src/task8_pageindex_vectorless.py (Commit 6730609, c17c060) | Done |
| Xây dựng giao diện UI (Streamlit) | Thiết kế và lập trình giao diện người dùng Streamlit cho phép người dùng hỏi đáp, xem trích dẫn và cấu hình tham số RAG | app.py (Commit c17c060) | Done |

Chỉ kê khai công việc có thể đối chiếu bằng file, commit, pull request, test hoặc kết quả evaluation.

## Quyết định kỹ thuật quan trọng

Mô tả tối đa hai quyết định mà bạn trực tiếp tham gia:

1. **Quyết định:** Cài đặt lớp RobustBM25Okapi kế thừa BM25Okapi để xử lý triệt để hiện tượng điểm IDF bị âm khi từ khóa xuất hiện ở nhiều văn bản.  
   **Lý do/evidence:** Bộ dữ liệu PUBG có nhiều thuật ngữ xuất hiện lặp lại ở đa số tài liệu (ví dụ: "PUBG", "người chơi", "trò chơi"). Thuật toán BM25 chuẩn sẽ gán điểm âm cho các từ này làm sai lệch thứ hạng kết quả, trong khi biến thể điều chỉnh giúp giữ điểm số luôn dương và ổn định.  
   **Trade-off:** Cần can thiệp vào logic tính toán nội bộ của thư viện rank_bm25 thay vì gọi trực tiếp API mặc định.

2. **Quyết định:** Áp dụng Reciprocal Rank Fusion (RRF) với hằng số k=60 thay vì cộng điểm chuẩn hóa (Linear Score Combination).  
   **Lý do/evidence:** Điểm cosine similarity từ Semantic Search (nằm trong khoảng [0, 1]) và điểm BM25 (không bị chặn trên) có phân phối rất khác nhau, việc chuẩn hóa tuyến tính dễ gây thiên lệch về một bên. RRF dựa trên thứ tự xếp hạng giúp kết hợp khách quan và ổn định hơn.  
   **Trade-off:** Mất thông tin về độ lớn khoảng cách tuyệt đối giữa các vị trí liền kề, nhưng đổi lại tính ổn định cao hơn hẳn.

## Kiểm thử và kết quả

- Test hoặc query tôi đã dùng: Chạy `pytest tests/test_contracts.py` kiểm tra toàn bộ contract của Task 5, 6, 7, 8; chạy lệnh `streamlit run app.py` để tương tác thực tế với giao diện người dùng.
- Kết quả trước/sau nếu có: Đạt 100% các bài kiểm tra contract của module retrieval; giao diện hiển thị câu trả lời với trích dẫn chi tiết theo từng nguồn văn bản kèm liên kết tương ứng.
- Lỗi đã phát hiện và cách xử lý: Khi người dùng nhập truy vấn rỗng hoặc truy vấn chỉ toàn ký tự phân tách, BM25 dễ bị lỗi chia cho 0; tôi đã thêm khối kiểm tra điều kiện biên và fallback an toàn trả về danh sách rỗng đúng chuẩn SearchResult.

## Điều còn hạn chế

- Một hạn chế cụ thể của phần tôi làm: Giao diện Streamlit hiện tại chưa hỗ trợ hiển thị biểu đồ phân tích trực quan mức độ đóng góp chi tiết của từng thành phần (BM25 vs Dense) trong điểm số RRF.
- Nếu có thêm thời gian, thay đổi đầu tiên tôi sẽ thực hiện: Bổ sung thanh trượt tùy chỉnh trọng số động giữa Dense và Lexical ngay trên giao diện Streamlit để người dùng so sánh trực quan hiệu quả truy xuất.

## Xác nhận đóng góp

Tôi xác nhận nội dung trên phản ánh đúng phần việc của mình và có thể giải thích hoặc chạy lại trong buổi demo.

- Ngày: 25/09/2026
- Tên thành viên: Phùng Quang Minh Huy
