# Individual contribution report

## Thông tin

- Họ và tên: Nguyễn Minh Hiếu
- Mã học viên: 2A202602669
- Nhóm: 67
- Repository/branch: origin/hieu, origin/main

## Phần việc đã thực hiện

| Module/deliverable | Việc tôi trực tiếp làm | File/commit/PR | Trạng thái |
|---|---|---|---|
| Task 1: Thu thập văn bản pháp lý | Thu thập và tải 4 văn bản pháp lý PDF chính thức của PUBG (>1KB/file) | src/task1_collect_legal_docs.py, data/landing/legal/*.pdf (Commit 4c3c1db) | Done |
| Task 2: Crawl tin tức & metadata | Viết script crawl 5 bài viết tin tức PUBG kèm đầy đủ trường url, title, date_crawled, content_markdown | src/task2_crawl_news.py, data/landing/news/*.json (Commit 4c3c1db) | Done |
| Task 3: Chuẩn hóa Markdown | Chuyển đổi toàn bộ dữ liệu PDF và JSON sang định dạng Markdown chuẩn hóa (>200 ký tự/file) | src/task3_convert_markdown.py, data/standardized/ (Commit 4c3c1db) | Done |
| Task 4: Chunking và Indexing | Xây dựng pipeline chia nhỏ tài liệu thành 488 chunks, hỗ trợ embedding và upsert vào ChromaDB | src/task4_chunking_indexing.py, chroma_db/ (Commit c17c060) | Done |

Chỉ kê khai công việc có thể đối chiếu bằng file, commit, pull request, test hoặc kết quả evaluation.

## Quyết định kỹ thuật quan trọng

Mô tả tối đa hai quyết định mà bạn trực tiếp tham gia:

1. **Quyết định:** Sử dụng RecursiveCharacterTextSplitter với chunk_size=500 và chunk_overlap=50 ký tự theo ranh giới đoạn văn bản.  
   **Lý do/evidence:** Văn bản pháp lý và tin tức cập nhật PUBG có cấu trúc mục lục và các đoạn điều khoản ngắn, việc cắt theo kích thước 500 ký tự giúp giữ trọn vẹn ngữ nghĩa của từng điều khoản/sự kiện mà không làm loãng context khi truy xuất.  
   **Trade-off:** Một số danh sách liệt kê dài hoặc bảng biểu chi tiết bị ngắt đôi sang hai chunk kế tiếp nhau, đòi hỏi phải có overlap để tránh mất ngữ cảnh.

2. **Quyết định:** Chuẩn hóa quy trình tạo ID ổn định cho từng chunk theo định dạng {document_id}::chunk-{index} trước khi upsert vào ChromaDB.  
   **Lý do/evidence:** Tránh tình trạng sinh ID ngẫu nhiên dẫn đến việc ghi đè hoặc trùng lặp dữ liệu khi chạy lại pipeline nhiều lần, đồng thời giúp dễ dàng truy vết nguồn tài liệu gốc trong quá trình retrieval.  
   **Trade-off:** Phải lưu thêm trường metadata phụ trợ và xử lý logic sinh ID nhất quán giữa các tiến trình chạy lại.

## Kiểm thử và kết quả

- Test hoặc query tôi đã dùng: Chạy `pytest tests/test_acceptance.py` cho các test corpus và standardized; chạy `pytest tests/test_contracts.py` kiểm tra schema đầu ra của task 1 đến task 4.
- Kết quả trước/sau nếu có: Thu thập thành công 4 tài liệu pháp lý PDF (>50KB/file), 5 tin tức JSON đầy đủ metadata; chuyển đổi thành công 9 file Markdown chuẩn hóa; chia nhỏ và đánh chỉ mục thành công 488 chunks vào ChromaDB.
- Lỗi đã phát hiện và cách xử lý: Khi trích xuất nội dung từ PDF ban đầu gặp lỗi thiếu ký tự tiếng Việt có dấu và mất cấu trúc bảng; tôi đã xử lý bằng cách kết hợp thư viện MarkItDown và pypdfium2 cùng bộ lọc biểu thức chính quy để làm sạch văn bản trước khi lưu.

## Điều còn hạn chế

- Một hạn chế cụ thể của phần tôi làm: Chunking cố định theo số lượng ký tự (fixed-size) chưa nhận diện thông minh cấu trúc phân cấp (header H1, H2, H3) của tài liệu pháp lý phức tạp.
- Nếu có thêm thời gian, thay đổi đầu tiên tôi sẽ thực hiện: Triển khai chiến lược Section-aware Markdown Chunking để chia nhỏ theo từng đề mục hoàn chỉnh nhằm nâng cao tính toàn vẹn của ngữ cảnh.

## Xác nhận đóng góp

Tôi xác nhận nội dung trên phản ánh đúng phần việc của mình và có thể giải thích hoặc chạy lại trong buổi demo.

- Ngày: 25/09/2026
- Tên thành viên: Nguyễn Minh Hiếu
